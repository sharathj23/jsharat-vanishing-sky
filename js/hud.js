/* =========================================================================
   hud.js — intentionally minimal.
   The sci-fi boot sequence + HUD telemetry were removed for the cinematic
   documentary tone. This stub keeps window.HUD.update() safe to call.
   ========================================================================= */
window.HUD = { update() {} };
